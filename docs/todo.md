# Content Follow-up

These items do not block the current release because the site hides or labels unknown values instead of inventing them.

- Replace internal `TODO` profile contact/publication fields after the relevant member supplies and approves them.
- Reconfirm time-sensitive admissions counts, current projects, collaboration organizations and contact details before the next admission cycle.
- Replace reused generic news images when event-specific authorized photographs are provided.
- Add newly published papers from traceable faculty records and verify DOI/title consistency.

Each completed item must update its source/review date and pass the release checks.
